# Analyzer K001 Task 016 server request/lookup result

Status: PASS

Task: `prototype-analyzer-analysis-request-and-lookup-v1`

Starting head: `9336e4dc2c3b747d0ad336e6a0820e7588100085`

Product implementation head: `f2f4e7015d4bd0c0d53dd6d03cf8793a7cae2ba2`

## Product result

The real `/analyze/` surface now supports an operator-configured,
development-only server transport while retaining its loopback/same-origin
browser boundary. The approved WSL -> Neotame -> Mannitol route invokes only
fixed, token-validated commands. Browser request fields travel on stdin, and
the accepted Node CLI remains responsible for normalization, computation
identity, queueing, native execution, parsing, public status/result,
deduplication, cancellation semantics, and analysis-view materialization.

The browser loaded accepted non-golden Task 012 GNU checker key
`sha256-a7b55f2d72d95d769c7c7428198242c3bfbd16ca92e596a51fce0aa72d7b9782`
as an already-completed server result. No native execution occurred. Its
accepted status hash remained byte-identical before and after lookup, and eight
checker candidates mounted through exactly one shared Results Viewer.

The selected missing request was GNU 1-ply cube analysis of
`PAAAICMAAAAAAA:cAngAAAAAAAE` with null dice. It is distinct from both accepted
K001 golden requests and is based on the accepted Node cube fixture rather than
the active benchmark lane. Mannitol Node published key
`sha256-298393d5bd857e4d1a14f759450efe495d5cfe8654d841a7106c1171a2655a4b`,
and the browser displayed `queued`, an actually observed Node `running` state,
and `complete`. The shared viewer rendered all three cube actions.

Exact resubmission produced a visible Node cache-hit disposition, the same key,
the same result and analysis-view hashes, and one native attempt before and
after repeat. Malformed browser input displayed `invalid`, sent zero adapter
submissions, ran no native process, and mounted no presentation.

## Product/durability boundary

Fresh display read Node public status/result and the Node-produced analysis
view immediately. It did not wait for Corpus, Canonical Parquet, D1, or another
serving index. The adapter created no durable result cache, D1 write, Canonical
writer, Corpus change, alternate schema, or second Results Viewer. Browser
engine execution and laptop worker execution are both absent.

The proof used an isolated Node-owned Task 016 runtime on Mannitol. Live workers
inherited nice value 10 after load/native-process reconciliation. No active
benchmark output was read or mutated, no accepted golden request was rerun, and
the accepted Task 012 runtime record used for lookup was not modified.

Earlier bounded non-golden proof candidates remain preserved in the isolated
Task 016 runtime; nothing was deleted or forced. Two failed closed on
inconsistent checker dice context, and three completed while the short-lived
`running` observer and final fixture choice were refined. The completion and
repeat counts reported above apply to the selected final request.

## Validation

- Headless Chromium product proof: PASS; accepted lookup, missing submission,
  queued/running/complete, repeat reuse, failure display, shared viewer, and
  zero page/console errors.
- Focused Analyzer/adapter/shared-viewer JavaScript and Python contracts: PASS.
- Full Quarto development render: PASS, 28/28 pages.
- Rendered static UI audit: PASS, 29 pages and zero findings.
- Practical quick build: JavaScript phases PASS; 84 Python tests ran with 81
  pass, one accepted historical scanner failure, and two skips.
- Full Python set: 219 tests ran with 215 pass, the same historical scanner
  failure, and three environment/negative-control skips.
- The publication-identity finding map remains exactly the accepted Task 013
  three-path debt; Task 016 introduces no scanner finding.
- Diff checks from the authorized starting head: PASS.

Result JSON SHA-256: `497d8b45af40b618f0e0bae92faed557e77d9583d505c67a03b60cee38a27942`

Required product blockers: 0
