# Repository-managed R dependencies for Analyzer board materialization.
# Names are package names and values are minimum supported versions.
required_r_packages <- c(
  jsonlite = "1.8.8",
  ggplot2 = "3.4.0",
  ggforce = "0.4.0"
)
